from enum import Enum

class GenerationMode(Enum):
    """Enumeration for different generation modes."""
    FAST = "fast"
    STANDARD = "standard"
    HIGH_QUALITY = "high_quality" # Example for future extension
